# Contributing

- Work happens on the `development` branch; open pull requests against `main`.
- Ensure linters and tests pass before submitting a PR.

Guidelines:
- Keep changes focused and well-documented.
- Add tests for new features or bug fixes.
- Run `pre-commit run --all-files` (ruff, black, isort) before committing.

Maintainer contact: see repository settings.
